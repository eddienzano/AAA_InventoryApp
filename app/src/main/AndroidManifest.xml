<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yourapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.YourApp"
        android:usesCleartextTraffic="true"

        >

        <!-- LoginActivity (no launcher intent, no need exported) -->
        <activity android:name=".LoginActivity" />

        <!-- IntakeActivity -->
        <activity android:name=".IntakeActivity" />

        <!-- Splash screen MainActivity (the ONLY launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">   <!-- ✅ required -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- DashboardActivity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true" />

<!--         ScanOutActivity (to be implemented)-->
        <activity
            android:name=".ScanOutActivity"
            android:exported="true" />

        <activity
            android:name=".FloorDashboardActivity"
            android:exported="true" />
        <activity
            android:name=".MainDashboardActivity"
            android:exported="true" />
        <activity
            android:name=".ScanOutReworkActivity"
            android:exported="true" />

        <activity
            android:name=".ScanOutWipActivity"
            android:exported="true" />

        <activity
            android:name=".ScanToBoxActivity"
            android:exported="true" />

        <activity
            android:name=".ScanToWipActivity"
            android:exported="true" />


        <activity
            android:name=".ScanBackColdroomActivity"
            android:exported="true" />

        <activity
            android:name=".NewIntakeActivity"
            android:exported="true" />
        <activity
            android:name=".RejectionActivity"
            android:exported="true" />

    </application>
</manifest>
