<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF8E1"
    android:padding="16dp">

    <LinearLayout
        android:id="@+id/scanBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:background="#FFECB3"
        android:elevation="4dp"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:gravity="center_horizontal"
        android:layout_gravity="center"
        android:orientation="vertical"
        >

        <!-- Scan Box -->

            <TextView
                android:id="@+id/titleText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🔧  Scan To Quarantine"
                android:textColor="#FF6F00"
                android:textSize="20sp"
                android:textStyle="bold"
                android:layout_marginBottom="12dp"/>

            <TextView
                android:id="@+id/subText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Scan the QR to Send it to Quarantine"
                android:textColor="#8D6E63"
                android:textSize="14sp"
                android:layout_marginBottom="20dp"/>


        <EditText
            android:id="@+id/qr_code_quarantine"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Scan or Enter QR Code"
            android:inputType="text"
            android:padding="12dp" />

        <Button
            android:id="@+id/scanBtnQuarantine"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Scan QR"
            android:padding="12dp"
            android:layout_marginTop="12dp" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="12dp">

            <TableLayout
                android:id="@+id/quarantineTable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:stretchColumns="*"/>

        </ScrollView>

    </LinearLayout>
</ScrollView>